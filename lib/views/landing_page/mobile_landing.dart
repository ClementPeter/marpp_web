import 'package:flutter/material.dart';

class LandingPageMobileView extends StatelessWidget {
  const LandingPageMobileView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Mobile Web-View coming soon...Switch to Desktop Site using Browser Setting',
      ),
    );
  }
}
